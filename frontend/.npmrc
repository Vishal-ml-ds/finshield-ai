# Treat mismatched peer deps as warnings, not errors.
# Required because @testing-library/* uses broad peer ranges (^18||^19)
# while React 19 patch versions have tighter internal constraints.
legacy-peer-deps=true
